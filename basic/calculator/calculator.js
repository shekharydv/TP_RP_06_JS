// Add function
function add(x, y) {
    let result = x + y;
    console.log(result);
}

add(1, 8);

// Add sub
function sub(x, y) {
    let result = x - y;
    console.log(result);
}

sub(1, 8);

// Multiply function
function multiply(x, y) {
    let result = x * y;
    console.log(result);
}

multiply(1, 8);

// divide function
function divide(x, y) {
    let result = x / y;
    console.log(result);
}

divide(1, 8);
